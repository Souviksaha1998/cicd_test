name: Build testing

on:
  push:
    branch:
      - master

jobs:
  build:
    name: Building on ubuntu image
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Installing requirements file
      run: |
          pip install -r requirements.txt

      
    
        
